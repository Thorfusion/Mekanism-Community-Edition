package mekanism.mekasuit.api.gear;

/** The equipment identities a MekaSuit module can be installed into. */
public enum ModuleTarget {
    MEKA_TOOL,
    HELMET,
    BODYARMOR,
    PANTS,
    BOOTS
}
